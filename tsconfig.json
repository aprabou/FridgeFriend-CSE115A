{
  "compilerOptions": {
    "baseUrl": ".",                    // ✅ Enables absolute paths
    "paths": {
      "@/*": ["src/*"]                // ✅ Adds @ alias
    }
  },
  "extends": "./tsconfig.app.json",
  "include": [
    "src",
    "vite.config.ts"
  ]
}
